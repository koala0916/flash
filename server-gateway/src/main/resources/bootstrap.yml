spring:
  application:
    name: server-gateway
  cloud:
    nacos:
      config:
        file-extension: yaml
        prefix: ${spring.application.name}
        server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
